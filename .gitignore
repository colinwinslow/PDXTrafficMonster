# Python bytecode cache.
__pycache__/
*.pyc

# Local per-session Claude Code state (do not commit).
.claude/settings.local.json

# telemetry add-on: local Claude Code token baseline + spend markers.
build/cc-token-usage/
